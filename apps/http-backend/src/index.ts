import express from "express";

const app = express();
app.use(express.json())

app.use("api/v1" , )
app.use("api/v1" , )


app.listen(3001 ,()=> {
    console.log('Server Running on the port: 3000')
})